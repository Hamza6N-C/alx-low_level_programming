#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - prints if the number is pos,neg or 0
 * Return: ALways 0
 */
int main(void)
{
	int a;
	char l;

	for (a = '0'; a <= 9; d++)
		putchar(a);
	for (l = 'a'; l <= 'f'; l++)
		putchar(l);
	putchar('\n');
	return (0);
}
