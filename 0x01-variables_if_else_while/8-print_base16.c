#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - prints if the number is pos,neg or 0
 * Return: ALways 0
 */
int main(void)
{
	int a;
	char l;

	for (d = '0'; d <= 9; d++)
		putchar(d);
	for (l = 'a'; l <= 'f'; l++)
		putchar(l);
	putchar('\n');
	return (0);
}
